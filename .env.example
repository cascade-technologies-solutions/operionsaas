# Environment Variables Configuration Template
# Copy this file to .env.production and fill in your production values
# For development, Vite will use the proxy configuration in vite.config.ts

# Backend API URL
# Include /api at the end as the API client appends endpoints to this base URL
# Example: http://your-backend-domain.com:3000/api
# Example: https://api.yourdomain.com/api
VITE_API_URL=http://3.107.223.34:3000/api

# WebSocket URL
# WebSocket server is attached to the same HTTP server with path /ws
# Use ws:// for HTTP or wss:// for HTTPS (if using SSL)
# Example: ws://your-backend-domain.com:3000/ws
# Example: wss://api.yourdomain.com/ws
VITE_WS_URL=ws://3.107.223.34:3000/ws